<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>//设置APP的整个色调
    </style>
    <style name="number_button">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_weight">1.0</item>
        <item name="android:layout_height">37dp</item>
        <item name="android:layout_margin">3dp</item>
        <item name="android:background">@drawable/select_number_click</item>
        <item name="android:textColor">#fff</item>//设置主界面的button的样式
    </style>
    <style name="MenuDialog" >
        <item name="android:windowBackground">@drawable/man_06</item>//背景
        <item name="android:windowFrame">@null</item>//边框
        <item name="android:windowNoTitle">true</item>//无标题
        <item name="android:windowIsFloating">true</item>//是否浮在activity上面
        <item name="android:windowIsTranslucent">true</item>//半透明
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:backgroundDimEnabled">true</item>//模糊
        <item name="android:background">@drawable/dialog_style</item>//背景样式

    </style>
</resources>
